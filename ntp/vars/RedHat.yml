---

# ntp packages
ntp_packages:
  - ntp
  - ntpdate

# remove conflicting packages
ntp_packages_removed:
  - chrony

# ntp service name
ntp_service: ntpd

# ntp main configuration
ntp_cfg: /etc/ntp.conf
