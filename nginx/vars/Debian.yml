---

# packages to install
nginx_packages:
  - nginx

# nginx service binary
nginx_binary: /usr/sbin/nginx

# nginx service
nginx_service: nginx

# nginx configuration directory base
nginx_cfg_base: /etc/nginx

# directory for vhosts
nginx_vhosts_dir: '{{ nginx_cfg_base }}/conf.d'

# nginx user
nginx_user: www-data
